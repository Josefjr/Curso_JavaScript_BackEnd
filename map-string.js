/* Desafio e deixar a lista de nomes todos em caixa Alta
o comando toUpperCase */

const nomes = ['chaves', 'chiquinha', 'nhonho', 'KIKO'];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase());

console.log(nomesPadronizados);