const alunos = ['José', 'Jennifer', 'Jonas', 'Aline'];
const medias = [10, 8, 7.5, 9];

const lista = [alunos, medias];
/* a primeita paste quando chama a lista e posição da lista no caso que a primeira posição e "0" 
a segunda posição e posição dentro da lista chamada no saso e a segunda "1" */
console.log(
    `A aluna da posição 1 da lista é: ${lista[0][1]}.
    A nota dessa Aluna é ${lista[1][1]}`
);

console.log(lista[1]);